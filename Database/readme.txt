Database class implemented in c++. Provides ability to add entry, remove entry, retrieve entry, and retrieve all entries in search-key order.

All entries contain two search keys, can search by either of these search keys to retrieve/remove entry. 

The database utilizes two binary search trees to contain the position of the data in an adt list. 